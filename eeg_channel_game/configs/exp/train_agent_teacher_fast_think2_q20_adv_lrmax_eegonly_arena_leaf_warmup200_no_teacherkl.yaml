base: train_agent_teacher_fast_think2_q20_adv_lrmax_eegonly_arena_leaf_warmup200.yaml

project:
  out_dir: runs/agent_bd_teacher_fast_think2_q20_adv_lrmax_eegonly_arena_leaf_warmup200_no_teacherkl

train:
  # Ablation: remove lr_weight teacher KL guidance (one-lever change).
  teacher_kl:
    enabled: false

