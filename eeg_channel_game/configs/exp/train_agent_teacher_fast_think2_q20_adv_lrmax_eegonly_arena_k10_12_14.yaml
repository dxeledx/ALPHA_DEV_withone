base: train_agent_teacher_fast_think2_q20_adv_lrmax_eegonly_arena.yaml

project:
  out_dir: runs/agent_bd_teacher_fast_think2_q20_adv_lrmax_eegonly_arena_k10_12_14

train:
  # Focus this iteration on the paper-critical region first (K=10/12/14),
  # then bring back the full Pareto later once we reliably beat full22 here.
  b_max_choices: [10, 12, 14]

