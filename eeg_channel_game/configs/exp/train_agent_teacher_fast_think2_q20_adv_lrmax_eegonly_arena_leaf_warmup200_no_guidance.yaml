base: train_agent_teacher_fast_think2_q20_adv_lrmax_eegonly_arena_leaf_warmup200.yaml

project:
  out_dir: runs/agent_bd_teacher_fast_think2_q20_adv_lrmax_eegonly_arena_leaf_warmup200_no_guidance

# Ablation: disable all lr_weight-based guidance mechanisms (one conceptual lever).
train:
  teacher_kl:
    enabled: false

mcts:
  policy_prior:
    enabled: false
  leaf_bootstrap:
    enabled: false

