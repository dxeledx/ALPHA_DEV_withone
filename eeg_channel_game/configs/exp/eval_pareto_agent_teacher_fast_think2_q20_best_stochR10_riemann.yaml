base: eval_pareto_agent_teacher_fast_think2_q20_last_riemann.yaml

eval:
  pareto:
    checkpoint: best
    tag: eval_best_stochR10_riemann

    # Stronger search during evaluation to avoid deterministic collapse.
    ours_restarts: 10
    ours_stochastic: true
    ours_tau: 0.8
